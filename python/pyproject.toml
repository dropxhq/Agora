[project]
name = "agora-a2a-demo"
version = "0.1.0"
description = "A2A mock server for Agora client debugging"
requires-python = ">=3.10"
dependencies = [
    "a2a-sdk[fastapi]>=1.0.0",
    "uvicorn[standard]>=0.32.0",
]

[tool.uv]
package = false
